<%= form_with(model: preferences_user, local: true) do |form| %>
  <% if preferences_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(preferences_user.errors.count, "error") %> prohibited this preferences_user from being saved:</h2>

      <ul>
        <% preferences_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :preferences_id %>
    <%= form.number_field :preferences_id %>
  </div>

  <div class="field">
    <%= form.label :users_id %>
    <%= form.number_field :users_id %>
  </div>

  <div class="field">
    <%= form.label :options %>
    <%= form.number_field :options %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
